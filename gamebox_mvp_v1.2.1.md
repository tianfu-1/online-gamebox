# 🎮 在线游戏集合网站需求文档（MVP 最终版 v1.2.1）

> 本版本在 v1.2 基础上确认：**分类页采用方案 A 的 Infinite Scroll**（每次加载 20 条，最多 160 条，不再分页）；推荐区数量维持 20；修正 JSON-LD；补充 SEO 细则；兼容第三方分发站 iframe。

---

## 一、项目概述

**目标：** 构建类似 **Poki.com** 的在线 HTML5 游戏集合网站，通过 **Google SEO** 获取美国市场自然流量。

**阶段：** MVP（最小可行产品）。

**核心功能：**
- 游戏展示与分类体系；
- 首页、分类页、游戏介绍页、游玩页；
- **热门筛选版推荐逻辑（每次推荐 20 个）**；
- 基础 SEO 优化；
- 分类页 **Infinite Scroll 方案 A**。

**技术栈：**
- 前端：Next.js + TailwindCSS
- 数据：JSON 文件（后期可接数据库）
- 存储：Cloudflare R2
- 托管：Cloudflare Pages

---

## 二、网站结构与功能模块

### 1️⃣ 首页 `/`
**目标：** 展示主要游戏集合与分类入口。

**结构：**
- 顶部导航：Home, Categories（下拉）, Popular, New Games, About / Contact
- H1（SEO）：`Play Free Online Games on {SiteName}`
- 展示区块：Popular / New / 精选分类

---

### 2️⃣ 分类页 `/category/[slug]/`（方案 A：Infinite Scroll）
**目标：** 展示该分类所有游戏，良好体验与可抓取的首屏内容。

**行为规范：**
- **首屏 SSR 输出 20 条**（可被爬虫抓取）；
- 使用 **IntersectionObserver**，每次滚动触底 **追加 20 条**；
- **最大加载上限 160 条**（8 批）。达到上限后展示提示：“You’ve reached the end.”；
- 若该分类总数 **不足 160 条**，则加载至 **全部展示完毕**；
- **不再提供分页**（无 `?page=` 分页链接）；
- 滚动加载期间，顶部 URL 保持不变（避免重复索引）。

**UI 结构：**
- H1：分类名
- 分类简介（80–120 字）
- 游戏卡片栅格（首屏 20 条 + 逐批追加）
- 触底 sentinel 元素（仅用于 IO）

**性能与无障碍：**
- 所有缩略图 `loading="lazy"`，写死 `width/height`；
- 逐批渲染时保留占位，降低 CLS；
- 对于禁用 JS 的场景：展示首屏 20 条与一条提示（“Enable JavaScript to load more”）。

---

### 3️⃣ 游戏介绍页 `/game/[slug]/`
**目标：** 介绍内容、玩法与点评，引导进入游玩页。

**结构与字数：**
- **H1**：游戏名称（含关键词）
- **封面图**
- **Main Description**（100–150 字）
- **How to Play**（150–200 字）
- **Editor’s Review**（200–250 字）
- **Play Now** 按钮 → `/play/[slug]/`
- **推荐游戏栏（侧边）**：**20 个**（同分类优先，基于 HotPool）

**总字数：** 500–600 字。  
**样式：** 每段以 `##` 小标题标注（便于扩写与 SEO）。

---

### 4️⃣ 游戏游玩页 `/play/[slug]/`
**目标：** 可玩、可全屏、安全、含推荐。

**布局：**
```
-----------------------------------
| 推荐栏 |     游戏 iframe 窗口    |
-----------------------------------
|        推荐游戏区块（下方）      |
-----------------------------------
```

**功能规范：**
- 通过 iframe 加载 Cloudflare R2 或第三方分发站地址；
- 默认非全屏；提供 **Fullscreen** 按钮（`requestFullscreen()`/`exitFullscreen()`）；
- `iframe` 属性：
  - `allowFullScreen`
  - `allow="fullscreen; gamepad; autoplay; encrypted-media; clipboard-read; clipboard-write; screen-wake-lock; xr-spatial-tracking"`
  - `sandbox="allow-scripts allow-same-origin allow-pointer-lock allow-popups allow-top-navigation-by-user-activation"`
- **推荐区数量：20 个**（逻辑同介绍页）；
- 图片与卡片懒加载，写死宽高。

**第三方游戏 iframe 兼容与安全**
- 仅嵌入未设置 `X-Frame-Options: DENY/SAMEORIGIN` 且 CSP 允许被嵌入的来源；
- 若受限无法嵌入，提供“在新窗口打开”降级方案；
- 设置站点级 `Permissions-Policy` 与 CSP，白名单允许的 `frame-src/img-src/media-src`；
- 第三方存储与 Cookie 可能受限，需验证游戏可用性；
- 跨域 iframe 无法直接注入埋点：进入游玩页时记一次 `play_count` 即可。

---

## 三、数据结构设计（`games.json`）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | number | 唯一 ID |
| title | string | 游戏名称 |
| slug | string | URL 标识 |
| thumbnail_url | string | 缩略图 |
| category | string | 分类 |
| description | string | 简介 |
| how_to_play | string | 玩法介绍 |
| editors_review | string | 编辑点评 |
| file_url | string | R2 或第三方地址 |
| play_count | number | 游玩次数（推荐池用） |
| created_at | date | 创建时间 |
| updated_at | date | 更新时间 |

> 可按分类拆分为多个 JSON（如 `games_action.json`），降低单文件体积。

---

## 四、推荐逻辑（热门筛选版，20 个）

**HotPool 构建：**
1. `play_count` 降序取 Top 50；
2. `created_at` 倒序取最新 50；
3. 合并去重为 `HotPool`。

**推荐规则（每次 20 个）：**
- 首选：从 **同分类 HotPool** 随机抽 **5 个**；
- 其余：从 **全站 HotPool** 随机抽 **15 个**；
- 不足则从全库随机补足；
- 当前游戏不参与推荐；
- HotPool 建议 24h 刷新一次缓存。

---

## 五、SEO 与元数据规范

### 1️⃣ Meta
- `<title>` ≤ 60 字符；`<meta name="description">` ≤ 160 字符；
- 每页仅一个 `<h1>`；补充 `<link rel="canonical">`。

### 2️⃣ Sitemap & Robots
- 生成 `sitemap.xml`（首页、分类、游戏介绍、游玩页；含 `lastmod`）；
- `robots.txt` 允许主要目录抓取。

### 3️⃣ Open Graph / Twitter Card
- `og:title`, `og:description`, `og:image`, `og:url`；`twitter:card=summary_large_image`。

### 4️⃣ 结构化数据（JSON-LD 示例，修正 @context）
```json
{
  "@context": "https://schema.org",
  "@type": "VideoGame",
  "name": "Subway Surfers",
  "description": "Play Subway Surfers, an action game where you run, dodge trains, and collect coins...",
  "genre": "Action, Endless Runner",
  "operatingSystem": "Web Browser",
  "applicationCategory": "Game",
  "image": "https://{site}/thumbnails/subway-surfers.jpg",
  "url": "https://{site}/game/subway-surfers",
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.7",
    "reviewCount": "12000"
  }
}
```

### 5️⃣ 性能细则（影响 SEO 的核心 Web Vitals）
- 所有缩略图：`loading="lazy"`，写死 `width/height` 或使用占位骨架；
- 若单页存在 20 张以上图片，启用 `srcset/sizes` 或多规格生成；
- 关键 CSS/字体预加载；避免阻塞渲染。

---

## 六、开发与部署建议

| 模块 | 方案 |
|---|---|
| 前端 | Next.js + TailwindCSS |
| 数据 | JSON（后期可换数据库） |
| 托管 | Cloudflare Pages |
| 存储 | Cloudflare R2 |
| 图片 | WebP + Lazy Load |
| 缓存 | Cloudflare CDN Cache Rules |
| 广告 | 介绍页中部、游玩页底部预留 AdSense 位 |

---

## 七、功能排期（MVP）

| 优先级 | 模块 | 说明 |
|---|---|---|
| ⭐⭐⭐ | 首页 + 分类页 | 分类页采用方案 A 的 Infinite Scroll |
| ⭐⭐ | 游戏介绍页 | 500–600 字 + 推荐 20 |
| ⭐⭐ | 游戏游玩页 | iframe + 全屏按钮 + 推荐 20 |
| ⭐ | 推荐逻辑 | HotPool（Top50 by play_count ∪ 最新50） |
| ⭐ | sitemap.xml | 快速收录 |

---

## 八、二期功能（延后）

| 模块 | 内容 |
|---|---|
| 内部链接至分类页 | 详情页底部链接至对应分类页 |
| 移动端沉浸式全屏 | 横竖屏自适配 |
| 用户收藏与评分 | 互动与留存 |
| 多语言 | EN / ES |
| 登录系统 | 用户数据与收藏同步 |

---

**版本号：** v1.2.1  
**日期：** 2025-10-24  
**维护人：** 产品负责人 + 前端负责人
